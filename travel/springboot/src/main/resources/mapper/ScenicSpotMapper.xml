<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springboot.mapper.ScenicSpotMapper">

    <select id="selectPage" resultType="com.example.springboot.entity.ScenicSpot">
        SELECT * FROM `scenic_spot`
        <where>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="location != null and location != ''">
                AND location LIKE CONCAT('%', #{location}, '%')
            </if>
            <if test="categoryId != null">
                AND category_id = #{categoryId}
            </if>
        </where>
        ORDER BY create_time DESC
        LIMIT #{offset}, #{size}
    </select>

    <select id="count" resultType="java.lang.Long">
        SELECT COUNT(*) FROM `scenic_spot`
        <where>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="location != null and location != ''">
                AND location LIKE CONCAT('%', #{location}, '%')
            </if>
            <if test="categoryId != null">
                AND category_id = #{categoryId}
            </if>
        </where>
    </select>

    <!-- 获取热门景点 -->
    <select id="selectHotScenics" resultType="com.example.springboot.entity.ScenicSpot">
        SELECT * FROM `scenic_spot`
        ORDER BY create_time DESC
            LIMIT #{limit}
    </select>

    <!-- 获取景点搜索建议 -->
    <select id="selectSuggestions" resultType="com.example.springboot.entity.ScenicSpot">
        SELECT id, name, location, price, image_url FROM `scenic_spot`
        WHERE name LIKE CONCAT('%', #{keyword}, '%')
            LIMIT #{limit}
    </select>

</mapper>